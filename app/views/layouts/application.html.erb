<!DOCTYPE html>
<html>
	<head>
		<title>Portfolio20</title>
		<meta name="viewport" content="width=device-width,initial-scale=1">
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>
		<%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
		<%= favicon_link_tag asset_path('shared/RV.svg') %>
		<%= javascript_importmap_tags %>
		<%= stylesheet_link_tag "https://cdn.jsdelivr.net/gh/devicons/devicon@v2.14.0/devicon.min.css" %>
	</head>
	<body class="background">
		<%= render "shared/nav" %>
		<%= yield %>
		<%= render "shared/footer" %>
	</body>
</html>
